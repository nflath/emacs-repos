int main (int argc, array(string) argv)
{
    options = (mapping) Getopt.find_all_options (argv,
						 ({
						     x
						 }));
    options = (mapping) Getopt.find_all_options (argv, ({
						     x
						 }));
}
