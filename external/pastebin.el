;;pastebin defines the commands M-x pastebin and M-x pastebin-buffer, which will send the region or buffer to a
;;pastebin, and place the URL of that pastebin as the last-copied item.

(require 'pastebin)