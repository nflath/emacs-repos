(defvar emacs-repos-dir "~/emacs-repos/"
  "Where the repository is located - used to load files ")

(defvar cpp-documentation-directory "~/emacs-repos/documentation/c++/"
  "Location of c++ documentation")

(defvar org-directory "~/org/"
  "Location of org files")

;;Set height
(set-face-attribute 'default nil :height 90)

(defvar man-dir "/usr/share/man/" "Location of man files")
(setq org-mobile-directory "~/tmp")